Dotnet ef migrations add --nameOfMigration
Dotnet ef migrations remove (removes all migrations)

DB: ID = GroomingAPI Password= Pa55w0rd!
dotnet ef database update -- updates the db.

Packages:
EntityFramework
AutoMapper ---DTO's

Profiles:
Help to set up the auto mapper feature to map Dto to model

Patch:
Is a part update instead of having to define the entire object.